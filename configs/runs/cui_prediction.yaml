# ── CUI embeddings (generated by data/CUI_embeddings.py) ──────────────────────
cui_embeddings:
  text_embeddings: "/home/ia368/projetos/imageclef2026-rag/artifacts/embeddings/IC2026_cui_mapping_text_embeddings.pt"
  ids:             "/home/ia368/projetos/imageclef2026-rag/artifacts/embeddings/IC2026_cui_mapping_ids.pt"

# ── Image embeddings (pre-computed, aligned with dataset order) ───────────────
image_embeddings: "/home/ia368/projetos/imageclef2026-rag/artifacts/embeddings/IC2026_medsiglip_valid_image_embeddings.pt"

# ── Dataset (used only to recover image_id order) ────────────────────────────
dataset: "/home/ia368/projetos/imageclef2026-rag/artifacts/datasets/imageclef2026_valid_dataset.json"

# ── Ground truth CUI labels ───────────────────────────────────────────────────
concepts_path: "/home/ia368/projetos/imageclef2026-rag/dev_caption/concepts.csv"

# ── Prediction threshold (ignored when --sweep is used) ───────────────────────
threshold: 0.25

# ── Two-phase sweep (only used with --sweep flag) ─────────────────────────────
# Range is derived automatically from the similarity distribution (p50 → p99).
# coarse_n: number of steps in the first pass (full range)
# fine_n: number of steps in the second pass (±fine_window around coarse best)
# fine_window: half-width of the fine search window
sweep_coarse_n:    40
sweep_fine_n:      50
sweep_fine_window: 0.025

# ── Output ────────────────────────────────────────────────────────────────────
output_dir:  "/home/ia368/projetos/imageclef2026-rag/artifacts/results/cui_prediction"
output_name: "cui_medsiglip_valid"
